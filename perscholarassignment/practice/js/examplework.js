let previous = 0
let current = 1
let nterm = 
console.log (previous);
console.log (current);

for(let counter = 1; counter <= 10; counter++){
    nterm = previous + current;
    console.log(answer);
    previous = current;
    current = answer;
}